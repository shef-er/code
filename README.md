# code

```shell
sudo pacman -S code

git clone git@github.com:shef-er/code.git ~/.config/Code

## for Code - OSS
ln -s ~/.config/Code "~/.config/Code - OSS"

## for VSCodium
ln -s ~/.config/Code ~/.config/VSCodium
```
